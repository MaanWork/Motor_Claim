import { NumbersdotsOnlyDirective } from './numbersdots-only.directive';

describe('NumbersdotsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumbersdotsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
